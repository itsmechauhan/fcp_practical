// Online C compiler to run C program online
#include <stdio.h>
#include<math.h>
int main() {
    //wap to find positive,negative,zero
    int n;
    printf("enter a no:");
    scanf("%d",&n);
    if(n>0)printf("+ive");
    else if(n<0)printf("-ve");
    else printf("zero");
    
    
    return 0;
}
